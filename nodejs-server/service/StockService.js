'use strict';


/**
 * Get stock price for a specific quote
 *
 * quoteId Long ID of quote to return
 * no response value expected for this operation
 **/
exports.getStock = function(quoteId) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Get stock price for all the quote
 *
 * no response value expected for this operation
 **/
exports.getStocks = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Update stock price for a quote
 *
 * no response value expected for this operation
 **/
exports.postStock = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

